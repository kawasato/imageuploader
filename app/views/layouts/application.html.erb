<!DOCTYPE html>
<html>
  <head>
    <title>ブログ</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' =>true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </head>
  <body>
    <header class="header">
      <div class="container">
        <ul class="row">
          <li class="col-sm-2 col-md-2"><a href="/blogs/new">新規投稿</a></li>
          <li class="col-sm-2 col-md-2"><a href="/blogs">ブログ一覧</a></li>
          
          <% if logged_in? %>
          <li class="col-sm-2 col-md-2"><%= link_to "プロフィール", user_path(current_user.id) %></li>
          <li class="col-sm-2 col-md-2"><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
          <% else %>
          <li class="col-sm-2 col-md-2"><%= link_to "新規登録", new_user_path %></li>
          <li class="col-sm-2 col-md-2"><%= link_to "ログイン", new_session_path %></li>
          <% end %>
          </ul>
          <% flash.each do |key, value| %>
            <%= content_tag(:div, value, class: "#{key}") %>
          <% end %>
      </div>
    </header>
            
    <%= yield %>
      
    <footer>
      <div id="footer">
        <p>201808期</p>
        <p>川崎智美</p>
      </div>
    </footer>
  </body>
</html>
